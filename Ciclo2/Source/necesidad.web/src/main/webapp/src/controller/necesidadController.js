define(['controller/_necesidadController','delegate/necesidadDelegate'], function() {
    App.Controller.NecesidadController = App.Controller._NecesidadController.extend({

    });
    return App.Controller.NecesidadController;
}); 