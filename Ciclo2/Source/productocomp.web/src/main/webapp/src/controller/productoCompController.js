define(['controller/_productoCompController','delegate/productoCompDelegate'], function() {
    App.Controller.ProductoCompController = App.Controller._ProductoCompController.extend({

    });
    return App.Controller.ProductoCompController;
}); 